export const buildPrompt = (text: string): string => {
  const t = text.trim();
  if (!t) return '（写経元の文章を入力してください）';

  return `以下の文章から、写経（書き写す）ことで理解が深まるような、本質的で短い文を5つ抜き出してください。

【条件】
- 意味が凝縮されていて、書き写す価値がある一文であること
- 元の文章の核心を捉えていること
- 前後の文脈がなくても、その一文だけで意味が通ること
- 番号付きリストで提示すること

文章:
「${t}」`;
};
